import { AssistancePipe } from './assistance.pipe';

describe('AssistancePipe', () => {
  it('create an instance', () => {
    const pipe = new AssistancePipe();
    expect(pipe).toBeTruthy();
  });
});
